{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "bootstrapPackage": "lerna bootstrap --include-dependencies --include-dependents --scope",
    "build": "lerna run build",
    "buildPackage": "lerna run build --include-dependencies --include-dependents --scope",
    "test": "lerna run test",
    "testPackage": "lerna run test --include-dependencies --include-dependents --scope",
    "prune": "lerna run prune",
    "buildAffected": "npm run buildPackage $(./affected.sh)",
    "testAffected": "npm run testPackage $(./affected.sh)",
    "publishCi": "lerna publish --conventional-commits -y"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.93",
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "lerna": "^5.1.4",
    "nx": "^14.3.6",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  }
}
